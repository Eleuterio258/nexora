# URL base da API do backend Nexora (Go) usada pelos repositórios HTTP
# (ver src/Infrastructure/Http/ApiClient.php). Sem esta variável, cai para
# http://localhost:8080.
NEXORA_API_BASE_URL=https://api.nexora.e258tech.tech
